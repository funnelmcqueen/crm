# Task 4 brief: operational localization

Translate the dashboard, pipeline, booking, and settings screens into English and German with the existing locale provider. Keep navigation, action payloads, record content, email addresses, and IANA time zone identifiers unchanged. Use locale-aware date and number formatting and preserve touch targets on phone layouts.

Implementation scope: a typed `operations` message namespace, relevant components and pages, and focused tests. Verification: operations/calendar tests, related dashboard/pipeline/booking regressions, typecheck, scoped lint, and diff check. This task is committed locally without pushing.

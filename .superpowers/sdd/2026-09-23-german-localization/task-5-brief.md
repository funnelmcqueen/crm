# Task 5 brief: admin localization

Localize the admin agent, phone number, report and CSV import screens in English and German. Use the existing locale provider and AppError message mapper. Keep CSV data and headers, action payloads, URLs, server error codes, and authorization behavior unchanged. Preserve mobile layouts and 48px controls.

Validation: focused admin/import/agent/i18n unit tests, typecheck, scoped ESLint, and diff check. Commit only Task 5 files on main; do not push.

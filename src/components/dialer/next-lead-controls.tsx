"use client";

// CONTRACT STUB: the stage 3 dialer agent replaces the implementation. Keep the export name and props.
// Rendered on the lead detail page when it was opened from the Next Lead flow (?flow=next).

export interface NextLeadControlsProps {
  leadId: string;
}

export function NextLeadControls(_props: NextLeadControlsProps) {
  return null;
}
